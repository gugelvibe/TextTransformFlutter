# General
.DS_Store
.dart_tool/
.packages
.pub-cache/
.pub/
build/

# Platform-specific shells (as requested to exclude build-related info)
# These can be regenerated with 'flutter create .'
android/
ios/
linux/
macos/
windows/

# Keep only shared assets if any
# assets/

# IDE files
.idea/
*.iml
.vscode/

# Localization/Logic to keep
# We keep lib/, test/, pubspec.yaml by not ignoring them or specifically including them
